providers:
  openai:
    api_key: ${OPENAI_API_KEY}
    auth_type: bearer

  deepseek:
    api_key: ${DEEPSEEK_API_KEY}
    auth_type: bearer

  claude:
    api_key: ${ANTHROPIC_API_KEY}
    auth_type: x-api-key

  github_copilot:
    api_key:
    auth_type: bearer

copilot:
  deployment: github.com
  enterprise_domain:
